version: 0.1
 
phases:
    install:
        commands:
            - npm install
            - npm install -g @angular/cli
            
    build:
        commands:
            # - npm run-script build
             - ng build --prod 
            
   artifacts:
  files:
     - '*'
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  base-directory: 'dist1'